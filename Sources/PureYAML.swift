/// Root namespace for the PureYAML package.
public enum PureYAML {
    /// YAML document model types.
    public enum Model {}

    /// YAML parsing types.
    public enum Parsing {}

    /// YAML emitting types.
    public enum Emitting {}

    /// YAML validation types.
    public enum Validation {}
}
